# Linker configuration for EhBASIC ROM
# Places BASIC at $8000-$BFFF (16KB)

MEMORY {
    BASIC: start = $8000, size = $4000, fill = yes, fillval = $FF;
}

SEGMENTS {
    CODE: load = BASIC, type = ro;
}
