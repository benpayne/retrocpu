# Linker configuration for BASIC ROM test program
# Maps code to $8000-$BFFF (16KB BASIC ROM region)

MEMORY {
    BASIC_ROM: start = $8000, size = $4000, type = ro, file = %O;
}

SEGMENTS {
    CODE: load = BASIC_ROM, type = ro;
}
